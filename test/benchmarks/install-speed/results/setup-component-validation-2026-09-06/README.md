# Setup component preparation check

This is the historical all-call logging candidate. Its subsequent TCG component timing regressed, and it was held from native measurement. The original source and result are preserved in `logging-bind/rejected-all-call/`; the selected follow-up limits the bind to serial system setup and requires new validation.

The native driver ran all four selected component contracts before actual payload composition: 13 indexing, 14 animation, seven firewall, nine logging-guard and 11 logging-payload/phase checks passed. The separate focused native log records five checks of selection, ordered preparation, distinct result paths and stale-input/failure handling.

This final check includes the finalized cached-loader, child-status and original-finally contracts. The exact source fingerprint covers 36 files and modes plus the pinned upstream commit. It includes the repository logger and license as well as both new component fixtures and dependencies. Successful contract-log hashes were rechecked before preparation. The base full-overlap manifest remains `0c7563d93d0ea95c2104bf7fbec2b50acb64dce304a32f1f55b54a6123729959`.

Actual preparation layered firewall scheduling and private logging binding. All 18 inherited files retained their bytes and modes. The final 30-file payload manifest is `ce2a26e6816a9c375217d5b154e5d55ef021f0439a2a5e81f4f282eed868cbc2`; final activation script SHA256 is `7d6f8fc57b6ba23c4a324e74da4ee9ea99b9260f23c9e6dfa73d45295c5ca339`. `preparation.json` records the exact native-driver hash, source fingerprint and log digests.

These are real host contract and composition checks, with controlled dependencies where the tests document them. No ISO was downloaded, VM started, target installed or installation speed measured in this check. Guest namespace checks and the fresh native trial remain separate evidence.
